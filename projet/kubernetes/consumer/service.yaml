
# kubernetes/redis/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: namouchi-bazzi-andriamasinoro
  labels:
    app: redis
    tier: cache
spec:
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
  type: ClusterIP